diff --git a/src/top-up/TopUp.sol b/src/top-up/TopUp.sol
index 8c3d182..4358fec 100644
--- a/src/top-up/TopUp.sol
+++ b/src/top-up/TopUp.sol
@@ -2,7 +2,7 @@
 pragma solidity ^0.8.28;
 
 import { IERC20, SafeERC20 } from "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";
-import { Ownable } from "solady/auth/Ownable.sol";
+import { Ownable } from "@openzeppelin/contracts/access/Ownable.sol";
 
 import { IWETH } from "../interfaces/IWETH.sol";
 import { Constants } from "../utils/Constants.sol";
@@ -27,11 +27,9 @@ contract TopUp is Constants, Ownable {
     event ProcessTopUp(address indexed token, uint256 amount);
 
     address public immutable weth;
+    error AlreadyInitialized();
 
-    constructor(address _weth) {
-        // initialize with dead so the impl ownership cannot be taken over by someone
-        _initializeOwner(address(0xdead));
-
+    constructor(address _weth)  Ownable(address(0xdead)) {
         weth = _weth;
     }
 
@@ -43,7 +41,7 @@ contract TopUp is Constants, Ownable {
      */
     function initialize(address _owner) external {
         if (owner() != address(0)) revert AlreadyInitialized();
-        _initializeOwner(_owner);
+        transferOwnership(_owner);
     }
 
     /**
